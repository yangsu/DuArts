li
    - var event = e.toObject();
    - var date = event.start.shortdate;
    a(href='event/#{event._id}', data-transition='slide-in', data-guid='#{event.guid}')
        | #{event.summary}

        //- if event.image
        //-     div
        //-         img.listimg(src='#{event.image}')
        span.chevron
        span.count= date
